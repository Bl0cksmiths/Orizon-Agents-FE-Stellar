# Mirrors of other repos — formatted by their own toolchains.
backend/
contract/
.canonical-contracts/
# Build and generated output.
.next/
coverage/
test-results/
playwright-report/
package-lock.json
docs/
whitepaper/
# Weekly submission bundles. Same reasoning as docs/: these are written for a
# grant reviewer, laid out by hand, and a formatter reflowing their tables and
# evidence blocks makes them worse. They are also frozen once submitted — a
# format sweep would rewrite a record of what was sent.
Week-1-Tranche-Submission/
