node_modules
.next
out
build
dist
.env
.env.local
.env.*.local
.DS_Store
*.log
.vercel
next-env.d.ts
.turbo


# Internal docs — litepaper & Instawards SOW/budget (kept local, not published)
whitepaper/
tsconfig.tsbuildinfo
coverage

# Playwright artifacts
test-results/
playwright-report/

# Lighthouse CI reports
.lighthouseci/

# Checkout of the contract repo's address book, compared against
# lib/contract-addresses.json by scripts/check-contract-addresses.mjs.
# CI writes it here; a local clone belongs here too, never committed.
.canonical-contracts/
